<Window x:Class="_3SC.Widgets.ApiStatus.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="API Monitor Settings"
        Width="400"
        Height="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <SolidColorBrush x:Key="BackgroundBrush"
                Color="#1E1E1E"/>
        <SolidColorBrush x:Key="BorderBrush"
                Color="#3F3F46"/>
        <SolidColorBrush x:Key="HeaderBrush"
                Color="#2D2D30"/>
        <SolidColorBrush x:Key="ForegroundBrush"
                Color="#E0E0E0"/>
        <SolidColorBrush x:Key="AccentBrush"
                Color="#0078D4"/>
        <SolidColorBrush x:Key="SecondaryBrush"
                Color="#2A2A2A"/>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{StaticResource HeaderBrush}"
                    CornerRadius="8,8,0,0">
                <TextBlock Text="⚙️ Settings"
                           Foreground="{StaticResource ForegroundBrush}"
                           FontSize="14"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           Margin="16,0"/>
            </Border>

            <!-- Content -->
            <StackPanel Grid.Row="1"
                    Margin="20">
                <TextBlock Text="Check Interval"
                           Foreground="{StaticResource ForegroundBrush}"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8"/>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Slider Grid.Column="0"
                            Minimum="30"
                            Maximum="600"
                            Value="{Binding CheckInterval}"
                            TickFrequency="30"
                            IsSnapToTickEnabled="True"
                            VerticalAlignment="Center"
                            Margin="0,0,12,0"/>

                    <TextBlock Grid.Column="1"
                               Text="{Binding CheckInterval, StringFormat={}{0} seconds}"
                               Foreground="{StaticResource ForegroundBrush}"
                               FontSize="12"
                               VerticalAlignment="Center"
                               MinWidth="80"/>
                </Grid>

                <CheckBox IsChecked="{Binding AlertOnDowntime}"
                          Foreground="{StaticResource ForegroundBrush}"
                          FontSize="12"
                          Content="🔔 Alert on API downtime"
                          Margin="0,0,0,12"/>

                <TextBlock Text="Alert notifications will be logged when an API goes down."
                           Foreground="#999999"
                           FontSize="11"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Buttons -->
            <Border Grid.Row="2"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,1,0,0"
                    Background="{StaticResource SecondaryBrush}"
                    CornerRadius="0,0,8,8"
                    Padding="16">
                <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                    <Button Content="Cancel"
                            Click="Cancel_Click"
                            Background="#555555"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="20,8"
                            Margin="0,0,8,0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver"
                                            Value="True">
                                        <Setter Property="Background"
                                                Value="#666666"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="Save"
                            Click="Save_Click"
                            Background="{StaticResource AccentBrush}"
                            Foreground="White"
                            BorderThickness="0"
                            Padding="20,8"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="4"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver"
                                            Value="True">
                                        <Setter Property="Background"
                                                Value="#1E88E5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
